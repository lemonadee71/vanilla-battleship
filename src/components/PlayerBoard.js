import { html } from '../component';
import Gameboard from '../modules/Gameboard';
import event from '../event';

const PlayerBoard = (size) => {
  const thisBoard = Gameboard(size);
};

export default PlayerBoard;
